#lang racket
(provide (all-defined-out))

;;; The sigil.
(define at-sign "@")

;;; The symbol used when margin notes are hidden on small screens.
(define margin-note-symbol "◊")
(define margin-figure-symbol "☙ ❧")


;;; Location of image assets.
(define image-location "/img")
